using System;
using System.Windows.Forms;
namespace Chapter3
{
    class Program
    {
        static void Main(string[] args)
        {
            // exiba a mensagem "enter two number" usando a classe MessageBox
            MessageBox.Show("Enter two numbers");

            // atribua o produto das variaveis b e c à variável a
            int a = 0;
            int b = 0;
            int c = 0;
            a = b * c;

            // diga que um programa efetua um exemplo de cálculo de folha de pagamento (isto é, use texto que ajude a documentar um programa
        }
    }
}